name: Sync Docs with Spitikos Backend

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  send-webhook:
    name: Send Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: List Doc Paths
        id: list_paths
        run: |
          paths=$(find . -type f -name "*.md" | sed 's|^./||' | jq -R . | jq -s -c .)
          echo "paths=$paths" >> $GITHUB_OUTPUT

      - name: Send Sync Request
        env:
          API_URL: https://api.spitikos.dev/docs.v1.DocsService/Sync
          PATHS_PAYLOAD: ${{ steps.list_paths.outputs.paths }}
        run: |
          curl --verbose \
            --header "Content-Type: application/json" \
            --data "{\"paths\": ${PATHS_PAYLOAD}}" \
            "${API_URL}"
